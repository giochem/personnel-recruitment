import { findVotesOfResume, createVote, updateVote, deleteVote } from '../services/vote.service';
import { reset } from '../reducers/vote.reducer';
export { reset, findVotesOfResume, createVote, updateVote, deleteVote };
